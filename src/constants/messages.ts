export const greetingsMsg = (name: string) => `Hi ${name}! 👋 This is GPT bot!`;
export const notAccessMsg = (name: string) => `The user <i>@${name}</i> does not have access to the bot`;
// export const resultMsg = (num: number) => {
//   const word = getNoun(num, 'результат', 'результата', 'результатов');
//   return `По Вашему запросу найдено ${num} ${word}:\n\n`;
// };

export const searchWaitMsg = 'Wait...';
export const invalidSearchRequestMsg = 'Invalid request, try again';
export const exitMsg = 'Bye! 🖖 To start the bot, enter: /start';
